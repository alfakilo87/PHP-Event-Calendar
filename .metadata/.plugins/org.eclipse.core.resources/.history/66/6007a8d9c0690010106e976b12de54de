<?php

class iCalendarEventParser
{
	var $events = false;
	var $paramarray = false;
	
	function setParamsToArray($file)
	{
		$f = fopen($file, 'r');
		while ($line = fgets($f))
		{
			print_r($line.'< br>');
		}
		fclose($f);
	}
}

?>
